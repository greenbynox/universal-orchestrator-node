# Frontend (Vite) env vars
# Copy to frontend/.env.local (or set via your shell) if you enable API auth on the backend.

# none | token | basic
VITE_API_AUTH_MODE=none

# Token mode
VITE_API_TOKEN=

# Basic mode
VITE_API_BASIC_USER=
VITE_API_BASIC_PASS=
